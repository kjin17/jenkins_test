---
apiVersion: apps/v1 
kind: Deployment
metadata:
  name: jenkins_test
spec:
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: jenkins_test
  replicas: 1 
  template: 
    metadata:
      labels:
        app: jenkins_test
    spec:
      containers:
      - name: jenkins_test
        image: kjin17/jenkinstest:latest
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: jenkins_test
  namespace: default
  labels:
    app: jenkins_test
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: jenkins_test
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
  kubernetes.io/ingress.class: nginx 
  name: jenkinstest_ingress
  namespace: default
spec:
  rules:
  - host: jenkinstest.localhost
    http:
      paths:
      - backend:
          service:
            name: jenkinstest
            port:
              name: http
        path: /
        pathType: Prefix
status:
  loadBalancer:
    ingress:
    - hostname: localhost
